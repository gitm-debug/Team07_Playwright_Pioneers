{
  "name": "lms-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "chrometest-noauth": "npx bddgen && npx playwright test --project=chromium --grep=@noauth",
    "chrometest-auth": "npx bddgen && npx playwright test --project=chromium --grep=@auth",
    "webkittest-noauth": "npx bddgen && npx playwright test --project=webkit --grep=@noauth",
    "webkittest-auth": "npx bddgen && npx playwright test --project=webkit --grep=@auth"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.2.0",
    "allure-playwright": "^3.10.2",
    "playwright-bdd": "^9.2.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
